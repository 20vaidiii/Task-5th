#Create a function that returns the factorial of a number using a while loop.
#(No recursion allowed)


def factorial(n):
    if n < 0:
        raise ValueError("Factorial is not defined for negative numbers.")
    
    result = 1
    while n > 1:
        result *= n
        n -= 1
    return result
